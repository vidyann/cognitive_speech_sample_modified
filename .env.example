# Azure Speech Service Configuration
# Copy this file to .env and fill in your values
# Command line arguments always take priority over .env values

# Speech Service credentials (Required for transcription)
SPEECH_KEY=your_speech_subscription_key_here
SPEECH_REGION=centralindia

# Language Service credentials (Optional - for conversation analysis)
# Only used when --useTextAnalytics flag is specified
LANGUAGE_KEY=your_language_subscription_key_here
LANGUAGE_ENDPOINT=your_language_endpoint_here

# Processing configuration (Optional)
# Container URL for batch processing
CONTAINER_URL=https://your_storage_account.blob.core.windows.net/container?sas_token

# Candidate locales for multi-language detection (comma-separated)
CANDIDATE_LOCALES=hi-IN,en-IN

# Output folder for local results storage
OUTPUT_FOLDER=results

# Output container URL for blob storage results (Optional)
#OUTPUT_CONTAINER_URL=https://your_storage_account.blob.core.windows.net/output?sas_token

# Example usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials and configuration
# 3. Batch processing with .env defaults:
#    python call_center.py
# 4. Override specific parameters:
#    python call_center.py --containerUrl "https://different_container..."
# 5. With conversation analytics:
#    python call_center.py --useTextAnalytics
#
# Note: 
# - Command line arguments override .env values
# - Conversation analytics requires --useTextAnalytics flag even if credentials are in .env
# - If CONTAINER_URL is set in .env, you can run batch processing without any command line args
